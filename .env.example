NODE_ENV=development

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=example
DB_NAME=playground

PORT=5000

# Optional. Set both to serve HTTPS directly instead of plain HTTP.
# Generate a self-signed pair for local use with:
#   openssl req -x509 -newkey rsa:2048 -nodes -days 365 \
#     -keyout certs/cert.key -out certs/cert.crt -subj "/CN=localhost"
# TLS_CERT_PATH=certs/cert.crt
# TLS_KEY_PATH=certs/cert.key
